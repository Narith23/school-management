<div class="row">
    <div class="col-12 mb-3">
        <a href="{{ $url }}" class="btn {{ $class }} btn-sm" title="{{ $title }}" class="btn btn-primary"><i
                class="{{ $icon }}"></i> {{ __("messages.create") }} {{ ucwords($title) }}</a>
    </div>
</div>
